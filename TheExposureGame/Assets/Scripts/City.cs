using UnityEngine;
using System.Collections;

public class City : MonoBehaviour
{

    public GameObject nextImage;

    // Use this for initialization
    void Start()
    {
        //LineRenderer lineRen = gameObject.AddComponent<LineRenderer>();
        //lineRen.SetPosition(1, nextImage.transform.position);
    }

    // Update is called once per frame
    void Update()
    {

    }
}
